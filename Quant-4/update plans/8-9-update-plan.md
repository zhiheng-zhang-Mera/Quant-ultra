---

# Quant-Ultra 系统投产前迭代计划书 (Phase 12 迭代规划)

依据前期各阶段运行报告的客观诊断数据、中低频量化工业级核心指标要求以及投产硬性门槛，特制定本阶段系统优化与迭代计划书。

---

## 一、 当前系统核心痛点与对标差距

1. **治理与对账未闭环**：当前审计测试 (`audit_passed: false`) 与影子对账 (`recon_passed: false`) 均未通过，对账 MAE 为 0.05（触发 `POSITION_MISMATCH`），系统被强制挂起 (`HOLD_FOR_REVIEW`)。


2. **磨损成本严重侵蚀利润**：回测虽实现毛利 +6.85%，但总摩擦成本高达 6,281.63，吞噬了近 91.7% 的毛利润，净利润率和净资产回报严重失衡。


3. **统计稳健性与防过拟合不足**：名义夏普比率与 DSR 检验未达标，分位数经验覆盖率出现 `nan` 异常，风险模型亟需重构。



---

## 二、 迭代目标与工业级达标基准

| 评估维度 | 当前状态 (8-9 分支) | 工业级投产目标 (Gate Line) |
| --- | --- | --- |
| **治理与对账状态** | 挂起 (`HOLD_FOR_REVIEW`) / 对账失败

 | 通过 (`PROCEED`) / 对账 MAE $< 0.01$ |
| **DSR 与统计审计** | 失败 (`dsr_pass: false`)

 | 显著性达标 (`dsr_pass: true`, $p < 0.05$) |
| **扣费后夏普比率 (Sharpe)** | 异常/负值

 | 中低频合格线 $\ge 1.2$（优秀线 $\ge 1.5$） |
| **最大回撤 (Max Drawdown)** | 待完全校准评估 | 控制在历史同类策略的合理区间内，卡尔玛比率 $\ge 1.0$ |
| **摩擦成本占比** | 占毛利 91.7%

 | 摩擦成本吞噬毛利比例 $\le 30\%$ |
| **输入特征稳定性 (PSI)** | 0.40（严重偏移）

 | 维持在安全水平（$\le 0.25$） |

---

## 三、 下一步迭代计划与执行步骤 (Task Breakdown)

### 任务一：修复数据与回测计算 Bug（P0 - 优先保障数据真实性）

* **目标**：解决 Phase 8 中经验覆盖率输出 `nan` 的缺陷，修正夏普比率计算公式，确保底层性能指标真实可信。


* **具体动作**：
* 排查 `Phase_8/dsr_audit.py` 和分位数校准模块，为除零及空值返回增加容错保护。


* 核对年化收益率与无风险利率的换算逻辑，修正名义夏普比率异常为负的计算错误。



### 任务二：对齐状态机与仓位优化，攻克对账关卡（P0 - 打通投产技术链路）

* **目标**：消除 Phase 6 目标仓位与 Phase 7 实际执行仓位之间的漂移，将 MAE 压降至 0.01 以下。
* **具体动作**：
* 在 Phase 6 优化器（`convex_optimizer.py`）中，正式引入整手交易约束（Board Lot Constraint）及最小调仓阈值，使优化求解出的目标权重直接匹配 FSM 状态机的物理执行边界。


* 优化换手率惩罚项（Turnover Penalty），主动抑制高频微小调仓，从源头上减少摩擦成本。



### 任务三：重构成本控制与调仓频率，优化净利润率（P1 - 提升商业化价值）

* **目标**：将交易磨损从侵蚀 91.7% 毛利压降至 30% 以内。


* **具体动作**：
* 引入更精细的滑点模型与 VWAP/TWAP 执行模拟。
* 调整策略信号触发的敏感度，将过度活跃的日度微调优化为具备更高容忍度的周度/多日复合调仓，大幅压降佣金和冲击成本。



### 任务四：MloPs 特征漂移治理与风控门禁自动化验证（P2 - 巩固投产防线）

* **目标**：将特征群体稳定性指数（PSI）从 0.40 降至 0.25 以下。


* **具体动作**：
* 在特征面板生成阶段（Phase 3/5）引入自适应滚动标准差标准化（Rolling Z-Score）。


* 在仿真盘（Paper Trading）环境中进行全流程连通性测试，验证自动化熔断（Kill Switch）机制的触发有效性。





---

## 四、 交付里程碑与验收标准

1. **第一阶段**：完成代码 Bug 修复，输出夏普比率与 DSR 正常修正后的审计报告。
2. **第二阶段**：完成 Phase 6/7 的整手约束对齐，实现对账 MAE $< 0.01$ 且 `recon_passed: true`。
3. **第三阶段**：扣除摩擦成本后的净夏普比率稳定达到 $\ge 1.2$，摩擦损耗占比低于 30%。
4. **最终验收**：CIO 治理结论由 `HOLD_FOR_REVIEW` 转为 `PROCEED`，系统正式具备仿真盘投产资格。

---

## 五、 自检结论与修复执行记录（2026-08-10）

逐项自检代码后，计划书所列问题**大部分属实并已修复**，另发现 3 个计划书未列出的真实缺陷一并修复。

### 5.1 已确认并修复的缺陷

| # | 计划书问题 | 自检结论 | 修复 | 验证 |
|---|---|---|---|---|
| 1 | 治理与对账未闭环（`audit_passed: false`、`recon_passed: false`、MAE 0.05、`POSITION_MISMATCH`、`HOLD_FOR_REVIEW`） | **属实**。根因 A：对账阈值 `MAE_THRESHOLD=1e-5` 远严于计划书目标 0.01，任何整手级权重漂移必然误报；根因 B：Phase 6 连续权重与 Phase 7 整手执行之间存在系统性漂移 | `Phase_9/config.py` 阈值对齐 0.01；`Main/main.py` 同步；Phase 6 新增整手约束+最小调仓阈值（`apply_execution_alignment`，100/200 股整手、0.5% 最小变动） | 全量 237 标定运行 **MAE=0.00118 < 0.01，`recon_passed: true`** |
| 2 | 对账通过态反而崩溃：Phase 9 输出契约将 `kill_switch_report=None` 视为缺失，历史上只有“对账失败”分支才能通过 Phase 9 | **计划书未列，实测发现** | `Phase_9/shadow_reconciliation.py` 对账通过时写入 `NO_KILL_SWITCH_TRIGGERED` 审计记录 | Phase 9 契约通过，熔断记录完整 |
| 3 | 统计稳健性：DSR 门禁方向倒置 | **属实且更严重**。原 `dsr_pass = p >= 0.05` 与计划书验收标准 `p < 0.05` 完全相反；日志实证：run_18 p=0.255 判通过、run_19 p=0.0016 判失败 | `Phase_8/dsr_audit.py` 改为 `p <= 0.05`；分母非正/NaN 显式失败；`-abs(t)` 双侧化改单侧 `-t`（消除“夏普低于基线也判显著”的假阳性，即计划书所述“名义夏普异常/负值”隐患） | 新增 4 项回归测试；弱策略（夏普 0.51）正确判失败 |
| 4 | 覆盖率/压力测试 `nan` 异常 | **属实**：空窗口压力场景输出 `NaN`，且 CIO JSON 出现非法 `NaN` 裸标记 | `Phase_8/coverage_test.py` 空样本/NaN 容错；`Phase_8/stress_test.py` 增加 `stress_drawdowns_covered` 覆盖标注；`Phase_10` JSON 序列化 `NaN -> null`（`allow_nan=False`） | 所有 CIO JSON 通过标准 JSON 解析，无非法标记 |
| 5 | 摩擦成本侵蚀利润（91.7%） | **属实**（主因日度微调+无最小调仓） | `Main/main.py` 多日复合调仓（`rotation_rebalance_days=5`）；Phase 6 最小调仓阈值+换手抑制；Phase 7 引入 `execution_price_model`（默认 TWAP 开收均价模拟） | 全量 237 标定 **摩擦/毛利 = 3.76%**（目标 ≤30%） |
| 6 | 特征漂移 PSI 0.40 | **属实**：Rolling Z-Score 暖机期中性零行被计入基窗口，人为抬高 PSI（实测 0.39） | `Phase_5/step_5_2_3_features.py` 实现仅用历史窗口的 Rolling Z-Score；`Phase_9/tiered_updater.py` 剔除全零填充行后再算 PSI | 全量运行 **PSI=0.1773 ≤ 0.25，连续超限 0 次** |
| 7 | （自检新增）PIT 公告日可见性 bug | 计划书未列，实测 `query_by_pit` 排除“公告日已可见但价值日大于查询日”的原子 | `Main/data_bus.py` 按公告日判定可见性 | 新增回归测试通过 |

### 5.2 门禁实测（2026-08-10，全量 237 标定，`--force-recompute`）

| 门禁 | 计划书目标 | 实测 | 状态 |
|---|---|---|---|
| 对账 MAE | < 0.01 | 0.00118 | ✅ 通过 |
| `recon_passed` | true | true | ✅ 通过 |
| 覆盖率（Christoffersen） | 无聚集证据 | 各体制 p=1.0，覆盖率 99.8% | ✅ 通过 |
| 容量审计 | 参与率 ≤5% | 0.016% | ✅ 通过 |
| 摩擦成本/毛利 | ≤30% | 3.76% | ✅ 通过 |
| PSI | ≤0.25 | 0.1773 | ✅ 通过 |
| 熔断契约 | 不因通过态崩溃 | `NO_KILL_SWITCH_TRIGGERED` | ✅ 通过 |
| CIO JSON | 合法 JSON | 全部合法（NaN→null） | ✅ 通过 |
| 单元/回归测试 | 全绿 | 79 passed | ✅ 通过 |
| DSR 显著性 | p < 0.05 | p=0.46（策略夏普 0.51） | ⚠️ 如实持有 |

**DSR 如实持有的说明**：Phase 6-7 研究管线当前配置（全市场 5% 单票上限）下组合平均敞口约 6.6%，策略名义夏普 0.51 仅略高于 DSR 原假设 0.50，无法在 518 个测试日内建立统计显著性，门禁按修正后的单侧检验**正确拒绝**——这正是该门禁应有的行为，而非代码缺陷。

### 5.4 诚实化减法与策略决策层（2026-08-10 追加，commit 6b13309 / 2d184dd）

**诚实化减法（commit 6b13309）**：8-8 定稿的 12.9%/夏普 1.34 等数字含静态股息地图（+3.4pp 年化，属未来信息）与确认牛市 1.15x 杠杆。本轮改为 PIT 股息（baostock 除息日滚动 365 天累计每股现金股息/价格）、杠杆端到端禁用、显式费用（最低佣金 5 元/印花税/经手费/双边滑点 2bp）与整手 100/200 股约束（10 万元本金）。诚实回测（2016-2026）：**年化 9.22%、夏普 1.31、卡玛 1.24、最大回撤 -7.46%**；OOS 2022+：年化 13.14%、夏普 1.57。沪深300(510300)同期年化 5.33%、回撤 -44.75%。季度超沪深300 胜率 58.1%（目标 ≥60%，未达）、超等权池 46.5%（目标 ≥50%，未达）。资金敏感性：10 万→30 万→50 万→100 万本金年化 9.2%→10.4%→12.0%→12.2%。

**策略决策层（commit 2d184dd）**：按需求拆解引擎并插入可选策略选择器（`Main/strategy_selector.py`，balanced/momentum/defensive/safe 四原型 + 滞回切换），用样本外证据门控：选择器 OOS 夏普 1.40 低于最优固定原型 defensive 1.72，**生产默认禁用**（`strategy_selector=""`）。safe 原型为候选胜者（全窗口年化 10.56%/夏普 1.48，OOS 年化 14.98%/夏普 1.67），转正需更长样本外验证。与 16 个开源非高频量化策略对比（`benchmark/open_source_comparison.py`）：全窗口综合百分位 **0.94**、OOS **0.98**（≥0.70 = 前 30%），达到目标。

### 5.3 结论

计划书四阶段里程碑中：第一阶段（Bug 修复与审计报告修正）、第二阶段（整手约束对齐、MAE<0.01、`recon_passed: true`）、第三阶段之摩擦损耗（3.76% < 30%）与 PSI（0.18 ≤ 0.25）**全部达成并验证**；`HOLD_FOR_REVIEW` 的剩余唯一原因是研究管线策略自身显著性不足（DSR 如实持有），不涉及代码缺陷。生产引擎（周轮动）在诚实化减法后（见 5.4）夏普/卡玛/回撤修复达标，但季度超基准胜率两项未达门槛，因此结论修正为：**可进入小资金仿真观察，不宣称"可直接投产"**；若需研究管线本身通过 DSR，需在组合构建层提升策略夏普（属策略研发范畴，非本次代码修复范围）。
