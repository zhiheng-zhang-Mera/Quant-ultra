# 8-14 离线评估与风险控制修复轮 (Offline Evaluation & Risk-Control Fix Round)

> 数据环境约束：本机缓存仅有 421 只（PIT 下载子集，中小盘为主），无网络、无全池 master list、无股息缓存。
> 因此本轮的"诚实回测"在**同一 421 只缓存池**上进行绝对口径自洽的相对对比，**不能**与 README 中 5475 只全池数字直接比较；
> 任何结论在接入全池数据后需复验（沿用项目证据门控文化）。

## 1. 验收基线（本轮修复前）

- 测试套件：`pytest Quant-4/tests` 实测 **121 passed / 3 failed / 32 errors**；
  - 失败与错误全部为**环境问题**而非代码缺陷：DSH 沙箱把 POSIX `0o700` 目录模式映射为拒绝访问的 DACL，
    导致 `tmp_path` fixture（32 项 error）与 `tempfile.TemporaryDirectory`（2 项 fail）在受限文件系统上不可用。
- 引擎（421 只缓存池，生产参数，10 万元本金、显式费用、整手、无杠杆）：
  **年化 2.29% / 夏普 0.28 / 卡玛 0.14 / 最大回撤 -16.92% / OOS(2022+) 3.75% / 0.40**。

## 2. 本轮修复与新增（全部默认关闭或字节兼容）

### 2.1 测试环境加固（conftest.py + 测试改造）

- `Quant-4/tests/conftest.py`：Windows 下将 pytest 临时目录模式改为宽松模式、`getbasetemp` 不再 `rm_rf`
  既有 basetemp 且使用工作区内根目录、`tempfile.TemporaryDirectory` 清理容错、dead-symlink 扫描容错。
  语义不变（fixture 隔离与顺序不变），CI（`quant4-ci.yml`，windows-latest）不受影响。
- `test_fundamental_factors.py`：2 个用例改用工作区内默认权限临时目录（`tempfile.TemporaryDirectory`
  在受限文件系统上创建即不可写）。
- 结果：**162 passed / 0 failed / 0 errors**（新增 6 个用例后）。

### 2.2 死代码清除（字节兼容，测试已验证）

- `Main/weekly_rotation.py::rank_candidates`：移除从未被使用的 `z_rows` 局部打分块（plan 8-11 已记录：
  `reversal_1d_weight`/`momentum_windows` 不参与实际打分，排名只消费 `composite_factor_scores`）。
  结果不变（`test_*` 全套回归通过）；`reversal_1d_weight` 标注为 legacy no-op。

### 2.3 真实缺陷修复：事件冲击被再平衡覆盖（风险优先序）

- 缺陷：**再平衡日当天，事件冲击触发的风险处置 pending 会被常规再平衡的 pending 无条件覆盖**，
  导致单日暴跌后的次日开盘仍按满仓目标执行，冲击保护在关键时点失效。
- 修复：冲击 pending 标记 `risk_off=True`，再平衡块（含中性/牛市 ETF 持有快捷路径）检测到
  `risk_off_pending` 时保留风险处置、跳过常规覆盖。
- 验证：新测试 `test_event_shock_zscore.py::test_zscore_rule_cuts_exposure_on_relative_crash`
  （暴跌后次日敞口从 0.95 降至 ~0.45，受单日换手上限 50% 约束——诚实的流动性建模）。

### 2.4 新增机制（默认关闭，生产行为字节不变）

| 参数 | 默认 | 说明 |
|---|---|---|
| `rebalance_min_turnover` | 0.0 | 常规再平衡的最小换手门槛：目标与当前持仓偏差过小时保持不动（带内再平衡），降成本/换手；风险态转换（熊/亢奋/冲击/回撤守卫）始终执行；首次建仓不跳过。 |
| `event_shock_zscore` | 0.0 | 稳健事件冲击检测：基准日收益低于其滚动均值 `z` 个标准差时触发风险处置（窗口 60 日）。固定 2.5% 阈值在小盘高波动池上误触发 158 次（实测），z-score 规则随波动自适应，跨池更稳健。与固定阈值可并存（任一触发即处置）。 |

## 3. 离线评估结果（421 只缓存池，生产参数为基线，35+ 配置网格，同一口径）

| 配置 | 年化 | 夏普 | 卡玛 | 回撤 | OOS 年化 | OOS 夏普 | 季度胜率(沪深300) |
|---|---|---|---|---|---|---|---|
| 基线（生产默认） | 2.29% | 0.28 | 0.14 | -16.92% | 3.75% | 0.40 | 62.8% |
| 事件冲击 3.0%（固定） | 2.94% | 0.35 | 0.19 | -15.33% | 5.41% | 0.57 | 62.8% |
| z-score 3.5 | 2.37% | 0.31 | 0.13 | -18.57% | 6.89% | 0.76 | 58.1% |
| **固定 3% + z-score 3.0** | **3.52%** | **0.40** | **0.24** | **-14.74%** | **6.35%** | **0.65** | **65.1%** |

- 关键诊断：421 只池基准（等权）单日跌幅 ≤-2.5% 的冲击日多达 **158 天**（2016 熔断、2018、2020、2024），
  2.5% 固定阈值近乎常开 → 平均总仓位仅 37%，牛市累计收益 -7.0%（小盘动量追高被反复收割）。
  3% 固定阈值 + z-score 3.0 双规则把误触发降到合理水平，全窗口与 OOS 同时改善。
- 研究证据门：以 35 次尝试注册 `num_trials` 后，DSR 多重检验 p 值在该子集上**诚实返回 HOLD_FOR_REVIEW**
  （OOS 夏普 0.65 不足以通过 35 次尝试的惩罚——门禁按设计工作）；全池 OOS 夏普 ~1.07 的口径下可通过。

## 4. 生产默认决策

- **生产默认参数保持字节不变**（`run_weekly_rotation.default_params()`）：上述改进为默认关闭的新机制
  + 修复风险优先序缺陷；参数级推荐（冲击阈值 3% + z-score 3.0）需要在全池 PIT 数据上复验后
  才能写入生产默认（沿用项目"证据门控、避免过拟合"原则）。
- 事件冲击优先序修复为纯质量修复，无参数含义，默认即生效。

## 5. 验证

```powershell
# 全套测试（含新增 6 项）
python -m pytest Quant-4/tests -q -p no:cacheprovider
# 结果：162 passed

# 离线评估复现（421 只缓存池）
python Quant-4/reports/_iter/run_sweep.py --configs baseline,z30_shock3 --trials 35 `
  --out Quant-4/reports/_iter/round1_verification.json
```

## 6. 限制披露

- 421 只缓存池为 PIT 下载的未完成子集（中小盘/退市为主，无大蓝筹、无避险 ETF、无股息、无 master list），
  与全池（5475 只）特征不同：基准波动远高于全池，收益绝对水平不可外推。
- 无网络：未复跑全池诚实回测；README 全池数字（6.25%/1.00/-7.65%）保持不变。
- 所有相对改进均在同一数据、同一会计口径下测量；接入全池后需重跑证据门。

---

# 第二轮（Round 2，2026-08-14）：止损带、稳健冲击检测的联合调优与 CLI 配置档

## R2.1 新增机制（默认关闭，字节兼容）

- `trailing_stop_pct`：峰值追踪止损（0=关）。开启后日内止损改为相对持仓峰值的回撤（峰值每日收盘抬高），
  让赢家奔跑、限制亏损；止盈带仍可独立生效。**实测在本小盘池上显著更差**（无止盈的宽追踪导致回撤翻倍至 -30%+，
  见 R2.4），故仅作可选机制保留，供趋势性更强的全池/其他市场复验。
- CLI `--profile`：`production`（默认，字节不变）与 `robust`（离线子集推荐：固定 3% + z-score 3.0 冲击检测 + 8% 止损带），
  为全池复验提供一键入口。`default_params()` 不变（新测试锁定）。

## R2.2 代码质量

- 删除 `weekly_rotation.py` 中未被使用的 `_execution_snapshot` / `_open_to_open_return`（向量化执行路径早已替代，字节兼容）。
- 修复报告生成器中的**过时且自相矛盾**文本：入门解读的"57 只大盘股/6% 止盈 8% 止损"改为 PIT 全池 + 12%/7%；
  "第三视角审查"章节原先引用的是 2026-08-10 旧口径数字（夏普 1.31/卡玛 1.24/9.2%），与上方记分卡矛盾，
  现改为**动态引用本次 summary** 并附当前生产口径结论与限制披露（不再硬编码历史数字）。

## R2.3 迭代网格（81+ 配置，同一 421 只池、同一会计口径）

| 配置 | 年化 | 夏普 | 卡玛 | 回撤 | OOS 年化 | OOS 夏普 | 季度胜率(沪深300) |
|---|---|---|---|---|---|---|---|
| 生产基线（含缺陷修复） | 2.80% | 0.32 | 0.17 | -16.69% | 4.84% | 0.49 | 62.8% |
| z-score 3.0 + 固定 3% 冲击（R1 最优） | 3.52% | 0.40 | 0.24 | -14.74% | 6.35% | 0.65 | 65.1% |
| **robust 档（+8% 止损）** | **5.25%** | **0.55** | **0.42** | **-12.59%** | **7.26%** | **0.70** | **65.1%** |
| 同上 + 10% 止损 | 4.92% | 0.51 | 0.32 | -15.34% | 4.65% | 0.47 | 67.4% |
| 同上 + 6% 止损 | 1.07% | 0.16 | 0.03 | -31.89% | 3.03% | 0.35 | 53.5% |

- 关键发现：7% 止损在本池上反复被噪声止损出局（换手/回撤反噬）；放宽到 8% 后收益与回撤同时改善
  （与全池 8-11 网格方向一致：12%/9% 档收益更高、7% 档回撤更小，8% 位于两者之间且配合稳健冲击检测最佳）。
- 已验证无效/更差的方向（如实记录）：动量门槛 0.01/0.02、persist 6/10、max_annual_vol 0.35/0.50、
  恢复均线 3/8、ml_bear_floor 0.20（0.40 的 OOS 7.54%/0.72 与 robust 在噪声内）、确认均线 0/120、
  波动率目标 0.15、min_adv 3e7、事件冲击敞口 0.30/0.50、追踪止损 10%/12%/15%（回撤翻倍）、
  止盈 10%（截断赢家）/15%（更差）。
- 81 次尝试注册下 DSR 门诚实返回 HOLD（子集 OOS 0.70 不足以通过多重检验惩罚）；全池复验是采纳前提。

## R2.4 验证

```powershell
python -m pytest Quant-4/tests -q -p no:cacheprovider   # 165 passed
python Quant-4/reports/_iter/run_sweep.py --configs baseline,z3s8 --trials 81 `
  --out Quant-4/reports/_iter/round2_verification.json
# 全池复验入口（接入数据后）：
python Quant-4/run_weekly_rotation.py --pit --profile robust --num-trials 81
```

## R2.5 生产默认决策

- 生产默认参数仍保持字节不变（`--profile production`）；`robust` 档为文档化推荐，需全池 PIT 复验后采纳。
- 测试套件 **165/165 全绿**（新增追踪止损 3 项 + robust 档 1 项）。

---

# 第三轮（Round 3，2026-08-14）：账务自洽审计、因子库负结果与套筒组合评估

## R3.1 账务自洽审计（核心逻辑验证，新增 4 项永久测试）

对回测会计恒等式做了运行时审计（`tests/test_accounting_consistency.py`）：

| 恒等式 | 结果 |
|---|---|
| 净值复利 eq[t]=eq[t-1]·(1+r) | **精确**（偏差 0） |
| 无杠杆：敞口 ∈ [0,1] | **通过**（最大 0.967） |
| 换手/成本非负 | **通过** |
| 无执行日敞口漂移界 \|Δexpo\| ≤ expo·20% | **精确 0 违规**（关闭日内止损时） |
| 止损退出计入成本但不计换手 | **确认**（330 天"有成本无换手"全部来自止损路径；设计合理——换手列度量再平衡活动，止损费用已入成本列） |

结论：**引擎会计完全自洽**；止损路径的"成本计入、换手不计"已在代码注释中明确记录。

## R3.2 因子库负结果（诚实记录，不采纳）

在 `robust` 基座上挂载开放因子（qlib Alpha158 子集）：

| 因子 | 全窗口 vs z3s8 (5.25%/0.55/0.42) | OOS |
|---|---|---|
| rsi14 0.10 | 4.79%/0.51/0.37（更差） | 6.71%/0.65 |
| idll20 0.10 | 3.46%/0.39/0.20（更差） | 6.90%/0.69 |
| rsi14+idll20 | 4.01%/0.44/0.29（更差） | 7.00%/0.69 |
| max_ret 0.10 | 4.48%/0.49/0.28（更差） | 7.66%/0.74 |
| illiquidity 0.10 | 3.33%/0.37/0.22（更差） | 7.14%/0.69 |

结论：现有复合因子组合在本池上已近最优；额外因子稀释分数（因子间共线性 + 本池特征），全部拒绝。行业轮动因子因缓存无完整行业映射（仅 3 只）无法评估。

## R3.3 套筒组合（40/30/20/10）评估

| 基准参数 | 年化 | 夏普 | 卡玛 | 回撤 |
|---|---|---|---|---|
| production | 1.14% | 0.21 | 0.10 | -11.69% |
| **robust** | **1.90%** | **0.32** | **0.15** | -12.56% |

`robust` 机制在套筒层同样生效（年化 +67%）；套筒组合回撤整体优于单书（分散化）。`run_sleeve_portfolio.py` 已同步增加 `--profile {production,robust}`。

## R3.4 结构验证

- 全部 6 个 CLI 入口（run_weekly_rotation / run_sleeve_portfolio / run_strategy_selector /
  run_adaptive_backtest / run_advice_portfolio_backtest / ab_llm_text_analysis）import 冒烟通过。
- 测试套件 **170/170 全绿**（新增账务审计 4 项）。

## R3.5 复现与采纳路径

```powershell
python -m pytest Quant-4/tests -q -p no:cacheprovider          # 170 passed
python Quant-4/run_weekly_rotation.py --pit --profile robust --num-trials 88   # 全池复验
python Quant-4/run_sleeve_portfolio.py --pit --profile robust --num-trials 88  # 套筒复验
```
