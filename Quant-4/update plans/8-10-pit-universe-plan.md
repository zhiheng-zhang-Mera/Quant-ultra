# PIT 无偏池改造与工业交付迭代计划 (2026-08-10)

## 1. 目标（用户最新指示）

1. **尽可能处理幸存者偏差**：轮动股票池不再使用 2026 年手工精选的 57 只，改为"全 A 股曾上市 + 退市股"的 PIT 池，成员资格以 baostock 主列表（ipo/out 日期）为准；
2. **调整优先级：低风险 → 快速增值**：先守住回撤（现有事件冲击/避险轮动/防御核心），再追求增值（safe 原型为候选）；
3. 允许个股与 ETF，**融资杠杆端到端禁用**；
4. 全方面改进后**回测 → 自检 → 迭代**，直到可作为工业项目交付。

## 2. 已完成（commit 3ac0a91）

- `Main/pit_universe.py`：主列表（5543 只股票、含 248 只窗口内退市）、`ever_alive_stocks`、`alive_matrix`、`universe_coverage` 审计；
- `Main/weekly_rotation.py`：`alive_mask` PIT 会员过滤（候选池+基准），退市/终止持仓按最后可用收盘价强平并记录成交（含卖出费用）；
- `run_weekly_rotation.py --pit`：无偏池回测入口 + 覆盖率报告 `universe_coverage.json`；
- `tools/download_universe.py`：增量可恢复批量下载器（东财→新浪→腾讯，熔断器+单实例锁，进度/失败落盘）；
- 测试：`tests/test_pit_universe.py` 7 项，全套 95 项通过。

## 3. 数据覆盖里程碑（下载进行中，后台进程）

| 门槛 | 目标 | 当前（2026-08-10 16:40） |
|---|---|---|
| 全池覆盖（5475 = 5453 股 + 22 ETF） | ≥95% | 7.5%（409/5475），后台 6 并发下载中（ETA ≈ 90 分钟） |
| 退市股覆盖（248 只） | ≥90% | 8.1%（20/248），主通道完成后用 baostock 慢速补跑 |
| PIT 股息覆盖 | 覆盖池内 ≥80% | 仅原 61 只，主通道完成后 baostock 补跑 |

## 4. 迭代顺序（每轮：回测 → 自检 → 门禁）

1. **数据闭合**：全池日线 ≥95%、退市股 ≥90%、股息覆盖披露；
2. **诚实 PIT 基准回测**：`--pit` 全池，披露 vs 沪深300、季度胜率、幸存者偏差残余（数据缺口）；
3. **低风险优先调参**：在 PIT 池上复跑 balanced/defensive/safe 原型 + 证据门控，选择"回撤更小且年化不降"的配置；保持无杠杆；
4. **工业门禁自检**：88+ 项测试全绿、覆盖率报告、资金敏感性（10万/30万/50万）、OOS 2022+、开源对比排名刷新；
5. **文档交付**：README/计划数字更新、限制披露（退市股结算近似、BSE 排除、股息缺口）。

## 5. 验收定义（工业交付）

- `--pit` 回测可复现，覆盖率 JSON ≥95% 且退市覆盖 ≥90%；
- 全窗口年化 ≥ 沪深300，季度超沪深300 胜率 ≥60%（或如实披露未达并给出原因）；
- 最大回撤维持 ≤10%（低风险优先）；
- 无任何杠杆路径（测试断言）；
- 全套测试通过，报告含覆盖率与限制章节。
